# ── core ───────────────────────────────────────────────
streamlit==1.36.0
pandas>=2.2,<3.0
numpy>=1.26,<2.0
python-dateutil>=2.9

# charts / viz
plotly>=5.22
altair>=5.3
yfinance>=0.2

# RL + data
gymnasium>=1.1,<2.0
stable-baselines3>=2.6,<3.0
pandas-datareader>=0.10

# LLM / API client
groq>=0.9

# AWS helper (SNS publish)
boto3>=1.34,<2.0

# optional (used by utils.create_pdf etc.)
fpdf>=1.7,<2.0

# keep torch CPU build; App Runner has no GPU
torch>=2.2,<3.0
